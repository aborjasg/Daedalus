# Microservices

Each directory is an independently deployable Flask API. Services should
communicate over HTTP or asynchronous events and should not access another
service's database tables directly.
