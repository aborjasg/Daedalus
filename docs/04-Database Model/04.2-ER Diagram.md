# ER Diagram (Text)

> "Project: Daedalus-Control Panel  
> DB: daedalus_controlpanel_db  

## Entities

TENANTS (tenant_id)
- name  
- status  
- created_at  
- created_by  
- updated_at  
- updated_by  

COMPANIES (company_id)
- tenant_id (FK → TENANTS.tenant_id)  
- legal_name  
- domain  
- timezone  
- created_at  

USERS (user_id)
- tenant_id (FK → TENANTS.tenant_id)  
- email  
- password_hash  
- status  
- last_login  
- created_at  
- updated_at  
- created_by  
- updated_by  

ROLES (role_id)
- tenant_id (FK → TENANTS.tenant_id)  
- name  
- description  
- status  
- created_at  
- updated_at  

PERMISSIONS (permission_id)
- code  
- description  

USER_ROLES
- user_id (FK → USERS.user_id)  
- role_id (FK → ROLES.role_id)  

ROLE_PERMISSIONS
- role_id (FK → ROLES.role_id)  
- permission_id (FK → PERMISSIONS.permission_id)  

MODULES (module_id)
- code  
- name  
- is_system_module  
- status  
- created_at  

TENANT_MODULES
- tenant_id (FK → TENANTS.tenant_id)  
- module_id (FK → MODULES.module_id)  
- status  
- billing_plan_id (FK → TENANT_PLANS.plan_id)  

MODULE_SETTINGS (setting_id)
- tenant_id (FK → TENANTS.tenant_id)  
- module_id (FK → MODULES.module_id)  
- key  
- value (JSONB)  
- updated_at  
- updated_by  

INTEGRATIONS (integration_id)
- code  
- name  

TENANT_INTEGRATIONS (tenant_integration_id)
- tenant_id (FK → TENANTS.tenant_id)  
- integration_id (FK → INTEGRATIONS.integration_id)  
- status  
- created_at  

INTEGRATION_SECRETS (secret_id)
- tenant_integration_id (FK → TENANT_INTEGRATIONS.tenant_integration_id)  
- key  
- value_reference  

TENANT_PLANS (plan_id)
- code  
- name  
- price_monthly  
- price_yearly  
- status  
- created_at  

TENANT_SUBSCRIPTIONS (subscription_id)
- tenant_id (FK → TENANTS.tenant_id)  
- plan_id (FK → TENANT_PLANS.plan_id)  
- status  
- renewal_date  

USAGE_METRICS (metric_id)
- tenant_id (FK → TENANTS.tenant_id)  
- module_id (FK → MODULES.module_id)  
- metric_code  
- value  
- timestamp  

AUDIT_LOGS (audit_id)
- tenant_id (FK → TENANTS.tenant_id)  
- user_id (FK → USERS.user_id)  
- action  
- entity  
- entity_id  
- timestamp  
- metadata (JSONB)  

SYSTEM_EVENTS (event_id)
- tenant_id (FK → TENANTS.tenant_id)  
- module_id (FK → MODULES.module_id)  
- event_type  
- payload (JSONB)  
- created_at  

---

## Relationships (Textual ERD)

TENANTS
- 1:N COMPANIES  
- 1:N USERS  
- 1:N ROLES  
- 1:N TENANT_MODULES  
- 1:N TENANT_INTEGRATIONS  
- 1:N TENANT_SUBSCRIPTIONS  
- 1:N USAGE_METRICS  
- 1:N AUDIT_LOGS  
- 1:N SYSTEM_EVENTS  

USERS
- M:N ROLES via USER_ROLES  

ROLES
- M:N PERMISSIONS via ROLE_PERMISSIONS  

MODULES
- M:N TENANTS via TENANT_MODULES  
- 1:N MODULE_SETTINGS  
- 1:N SYSTEM_EVENTS  
- 1:N USAGE_METRICS  

INTEGRATIONS
- M:N TENANTS via TENANT_INTEGRATIONS  
- 1:N INTEGRATION_SECRETS via TENANT_INTEGRATIONS  

TENANT_PLANS
- 1:N TENANT_MODULES  
- 1:N TENANT_SUBSCRIPTIONS  

> "Project: Daedalus-Tenant (Template)  
> DB: daedalus_tenant_<DB_Tenant_Name>_db

## Entities



## Relationships (Textual ERD)

